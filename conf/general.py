
LABEL_PREFIX = 'label'
PREVIOUS_PREFIX = 'previous'
CMAP_PREFIX = 'cmap'

VAL_PREFIX = 'val'
TRAIN_PREFIX = 'train'

PREDICTION_PREFIX = 'pred'

PATCH_SIZE = 128
OVERLAP_PROP = 0.7

N_IMAGES_YEAR = 3

N_OPTICAL_BANDS = 13
N_SAR_BANDS = 2
N_CLASSES = 3

CLASSES_WEIGHTS = [0.2, 0.8, 0.0]

LEARNING_RATE_SCHEDULER_GAMMA = 1
LEARNING_RATE = 5e-5
EARLY_STOP_PATIENCE = 10
EARLY_STOP_MIN_DELTA = 5e-5
EARLY_STOP_MIN_EPOCHS = 1
MAX_EPOCHS = 500

PREDICTION_OVERLAPS = [0.15, 0.2, 0.25, 0.3]
PREDICTION_REMOVE_BORDER  = 8

MIN_CLOUD_COVER = 0.30

RESUNET_DEPTHS = [32, 64, 128, 256]#[32, 64, 128, 256]#[64, 128, 256, 512]

SWIN_WINDOW_SIZE = [4, 4]
SWIN_PATCH_SIZE = [4, 4]
SWIN_SHIFT_SIZE = [2, 2]
SWIN_BASE_DIM=96
SWIN_N_HEADS = [3, 6, 12, 24]
SWIN_N_BLOCKS = [2, 2, 2, 2]

